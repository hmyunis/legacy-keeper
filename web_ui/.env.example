# Legacy Keeper Web UI Environment Variables

# API Configuration
# The base URL of the backend API (include trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api/

# Google OAuth Configuration (Optional)
# Get your client ID from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id-here

# Web Push (Optional fallback if backend public key endpoint is unavailable)
VITE_VAPID_PUBLIC_KEY=

# Note: This is an example file. Copy this to .env and fill in your actual values.
# The .env file is ignored by git for security.
